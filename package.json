{
  "name": "mocktest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "mock": "json-server --watch api/db.json --port 4000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.15.0"
  }
}
